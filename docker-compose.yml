services:
  app:
    build: .
    command: ["bun", "run", "dev"]
    network_mode: "host"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - .env.local
    environment:
      DATABASE_URL: postgresql://obelisk:obelisk_dev@localhost:5432/obelisk
      OLLAMA_URL: http://localhost:11434
      TYPESENSE_URL: http://localhost:8108
      TYPESENSE_API_KEY: ${TYPESENSE_API_KEY:-obelisk_typesense_dev}
    depends_on:
      postgres:
        condition: service_healthy
      typesense:
        condition: service_healthy

  postgres:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_DB: obelisk
      POSTGRES_USER: obelisk
      POSTGRES_PASSWORD: obelisk_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U obelisk -d obelisk"]
      interval: 5s
      timeout: 5s
      retries: 5

  typesense:
    image: typesense/typesense:30.1
    ports:
      - "8108:8108"
    volumes:
      - typesense_data:/data
    environment:
      TYPESENSE_DATA_DIR: /data
      TYPESENSE_API_KEY: ${TYPESENSE_API_KEY:-obelisk_typesense_dev}
      TYPESENSE_SNAPSHOT_INTERVAL_SECONDS: "3600"
      GLOG_minloglevel: "1"
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/8108' || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 15s

volumes:
  postgres_data:
  typesense_data:
