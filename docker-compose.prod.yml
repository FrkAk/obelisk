# Production overlay: builds and serves (instead of dev hot-reload)
# Use with: docker compose -f docker-compose.yml -f docker-compose.prod.yml up
services:
  app:
    command: ["sh", "-c", "bun run build && bun run start"]
